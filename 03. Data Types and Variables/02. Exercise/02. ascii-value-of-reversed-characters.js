function reversedCharacters(firstChar, secondChar, thirdChar) {
  console.log(`${thirdChar}${secondChar}${firstChar}`);
  console.log(`${thirdChar.charCodeAt()} ${secondChar.charCodeAt()} ${firstChar.charCodeAt()}`);
}

reversedCharacters('%', '2', 'o');